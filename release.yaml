schema_version: 1
system_version: 0.1.0-alpha.7
channel: alpha
status: released
runtime_bundle: source-tree
source_repository: jeremieguedj/School-OS
data_schema_compatibility:
  minimum: 1
  maximum: 1
required_capabilities: []
optional_capabilities:
  - scheduler.ensure
  - audio.generate
  - audio.retrieve
migrations:
  - migrations/0001-add-atomic-fact-refs-to-rolling-updates.md
notes: Adds a bounded supervised single-writer upgrade fallback for storage connectors without native write preconditions, preserving the frozen architecture and data schema.
