schema_version: 1
system_version: 0.1.0-alpha.8
channel: alpha
status: released
runtime_bundle: source-tree
source_repository: jeremieguedj/School-OS
data_schema_compatibility:
  minimum: 1
  maximum: 1
required_capabilities: []
optional_capabilities:
  - scheduler.ensure
  - audio.generate
  - audio.retrieve
migrations:
  - migrations/0001-add-atomic-fact-refs-to-rolling-updates.md
notes: Adds a scheduler-issued manual run-now bridge and simplifies routine coordination to one production sender plus delivery idempotency; data schema compatibility is unchanged.
