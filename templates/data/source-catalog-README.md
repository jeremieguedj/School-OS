# Source catalog

Create one canonical private record per configured source conversation or deliberately folded source-record group.

Each record must contain:

1. metadata and immutable source identities;
2. atomic facts with stable IDs;
3. ordered source coverage; and
4. raw available message text retained verbatim.

Do not store real catalog examples in the reusable package.
